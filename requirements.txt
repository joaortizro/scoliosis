# Core ML
torch
torchvision
scikit-learn
numpy
pandas
Pillow
opencv-python
scipy
torchxrayvision  # Phase 1.1 — PadChest+NIH+RSNA+SIIM+VinDr ResNet-50 weights
imageio          # torchxrayvision dep
scikit-image     # torchxrayvision dep
# ultralytics    # Phase 1.2 — only when Roboflow asset confirmed

# Experiment tracking
mlflow
dvc
dvc-s3  # swap for dvc-gdrive, dvc-ssh, etc.

# API server
fastapi
uvicorn[standard]
pydantic

# Utilities
python-dotenv
pyyaml
tqdm
matplotlib
seaborn

# Dev
jupyter
ipykernel
pytest
