fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.0
ultralytics>=8.2.0
torch>=2.2.0
torchvision>=0.17.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
Pillow>=10.3.0
# RB-UNet deps (mirror scoliosis-ai pyproject minus torch* already above)
scipy>=1.11
pandas>=2.2
PyYAML>=6.0
# Install the project's ai/ package in editable mode at image-build time.
# Dockerfile copies the repo root into /repo and runs `pip install -e /repo`.
